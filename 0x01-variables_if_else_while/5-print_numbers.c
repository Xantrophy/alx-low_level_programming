#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char letter;
for (letter = 0; letter <= 9; letter++)
{
printf("%d", letter);
}
printf("\n");
return (0);
}
